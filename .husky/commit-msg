#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "🧠 Checking commit message format..."
# Use npx to invoke local commitlint in a shell-friendly way across Windows/Git Bash
npx --yes commitlint --edit "$1"
